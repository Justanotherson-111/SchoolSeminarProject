FROM mcr.microsoft.com/dotnet/sdk:9.0

WORKDIR /app

# Copy project files
COPY . .

# Install dotnet-ef globally
RUN dotnet tool install --global dotnet-ef --version 9.0.0
ENV PATH="$PATH:/root/.dotnet/tools"

# Keep container running for EF commands
CMD ["tail", "-f", "/dev/null"]
